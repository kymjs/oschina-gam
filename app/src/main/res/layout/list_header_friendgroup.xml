<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                android:layout_width="match_parent"
                android:layout_height="210dp"
                android:background="@drawable/bg_headview">

    <ImageView
        android:id="@+id/frag_msg_img_zone"
        android:layout_width="match_parent"
        android:layout_height="210dp"
        android:layout_alignParentTop="true"
        android:contentDescription="@null"
        android:paddingBottom="27dp"
        android:scaleType="fitXY"
        android:src="@drawable/ic_zone"/>

    <TextView
        android:id="@+id/frag_msg_text_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/frag_msg_img_zone"
        android:layout_margin="5dp"
        android:layout_toLeftOf="@+id/frag_msg_img_head"
        android:paddingBottom="27dp"
        android:textColor="@android:color/white"
        android:textSize="20sp"/>

    <ImageView
        android:id="@+id/frag_msg_img_head"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:layout_alignBottom="@+id/frag_msg_img_zone"
        android:layout_alignParentRight="true"
        android:layout_marginRight="10dp"
        android:background="@android:color/white"
        android:contentDescription="@null"
        android:padding="2dp"
        android:scaleType="fitXY"
        android:src="@drawable/default_head"/>

</RelativeLayout>